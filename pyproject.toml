[project]
name = "sesac3th"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "daseinha", email = "daseinha@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
]

[project.scripts]
sesac3th = "sesac3th:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
